export default interface Charity {
    _id: string;
    every_id?: string;
    every_slug?: string;
    name?: string;
    description?: string;
    image_url?: string;
    website?: string;
    note?: string;
  }